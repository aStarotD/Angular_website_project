export interface ArticleMeta {
    title?: string,
    keyword?: string,
    description?: string
}

export interface ArticleAdItem {
    elem: string;
    insertAd?: boolean;
    adIndex?: number;
}
